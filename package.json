{
  "name": "coinbet",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ coinbetfi/slots-polygon --access-token $ACCESS_TOKEN",
    "create-local": "graph create --node http://localhost:8020/ coinbetfi/slots-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ coinbetfi/slots-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 coinbetfi/slots-polygon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
